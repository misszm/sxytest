1. 支持定时: 单个运行时间, 开始时间, 间隔时间, 结束时间, 每周每天定时运行, 精确到秒
2. 任务处理: 比如程序重启, 宕机等, 跳过了多次运行时间, 但是调度器会记录错过的运行任务, 再次重启也可以运行, 也可以不运行,
也可以只运行一次
3. 用Python subprocess执行服务器 任务启动命令
4. 存储器默认 只存储任务下次执行时间和任务状态, 可以存入mysql
5. 考虑问题: 命令执行结果不是预期的处理, 存储器怎么自动多存入信息